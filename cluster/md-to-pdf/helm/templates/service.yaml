apiVersion: v1
kind: Service
metadata:
  name: {{ include "md-to-pdf.fullname" . }}
  labels:
    {{- include "md-to-pdf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "md-to-pdf.selectorLabels" . | nindent 4 }}
