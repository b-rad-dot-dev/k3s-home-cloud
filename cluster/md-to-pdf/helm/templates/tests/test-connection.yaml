apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "md-to-pdf.fullname" . }}-test-connection"
  labels:
    {{- include "md-to-pdf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "md-to-pdf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
