apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webtop.fullname" . }}-test-connection"
  labels:
    {{- include "webtop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webtop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
