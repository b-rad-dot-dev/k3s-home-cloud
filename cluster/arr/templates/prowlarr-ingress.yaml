{{- if .Values.prowlarr.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "prowlarr.fullname" . }}
  labels:
    {{- include "prowlarr.labels" . | nindent 4 }}
  {{- with .Values.prowlarr.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.prowlarr.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.prowlarr.ingress.tls }}
  tls:
    {{- range .Values.prowlarr.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.prowlarr.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "prowlarr.fullname" $ }}
                port:
                  number: {{ $.Values.prowlarr.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
