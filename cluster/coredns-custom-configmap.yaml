apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  gitea.server: |
    gitea.b-rad.dev {
        hosts {
            10.0.128.0 gitea.b-rad.dev
            fallthrough
        }
    }