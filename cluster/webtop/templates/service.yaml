apiVersion: v1
kind: Service
metadata:
  name: {{ include "webtop.fullname" . }}
  labels:
    {{- include "webtop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 8082
      targetPort: websocket
      protocol: TCP
      name: websocket
  selector:
    {{- include "webtop.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "webtop.fullname" . }}-vite
  labels:
    {{- include "webtop.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  ports:
    - port: 5173
      targetPort: vite
      protocol: TCP
      name: vite
  selector:
    {{- include "webtop.selectorLabels" . | nindent 4 }}